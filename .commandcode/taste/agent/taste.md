# agent
- Detect user patterns (merchant→category mappings, recurring transactions) and confirm with user before acting. Never re-ask a dismissed suggestion — respect both accept and dismiss decisions permanently. Auto-apply confirmed patterns thereafter. Confidence: 0.75
- In the UI, only show active rules — do not display pending suggestions or dismissed history. Confidence: 0.70
- Auto-log rules belong in their own dedicated tab (Recurring), not on the Settings page. Confidence: 0.70
- Two distinct notification types for recurring patterns: (1) Timed log suggestions — trigger at the pattern's specific day/time asking "wanna log this again?" (e.g., breakfast at 8:30am Wed/Fri). (2) Rule-creation suggestions — for consistent patterns like rent, ask "wanna create a rule?" and can fire anytime. Confidence: 0.75
- Pre-generate AI notification content variants and cache them — do not call the AI live at notification time. The AI generates natural-language notification bodies when the pattern is first detected, and notifications pull from the cache. Keep a static fallback for when the cache is empty. Confidence: 0.70
- Static/fallback notification and event body text must use conversational, natural-language phrasing with proper formatting (₹ symbol, complete sentences). Never emit robotic templates like "Detected 15 Food on saturday" or "I'll remind you." For timed-log: ask directly if user wants to log (e.g., "Breakfast again? Wanna log ~₹200 on Food?"), since the pattern detection implies the user is likely adding this expense now. For rule-creation: ask if they want a recurring rule (e.g., "You've spent ~₹500 on Grocery at BigBasket a bunch of times. Create a recurring rule?"). Confidence: 0.75
- Notification text should incorporate the transaction's description/note (not just category and merchant) to mirror the user's own phrasing. For example, if a user consistently logs "130 for auto for office", the notification should say "Auto again? Wanna log 130 for auto to office?" — using the user's own description as conversational context, not just the bare category name. Confidence: 0.70
- Timed log suggestions should only appear in push notifications and the suggestion chip/strip — NOT in the chat view list. Recurring auto-log rule suggestions are the only recurring-pattern type that may appear in the chat view list. The suggestion strip should mirror push notification content exactly. Confidence: 0.75
